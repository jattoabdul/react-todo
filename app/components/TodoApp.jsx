var React = require('react');

var TodoApp = React.createClass({
    render: function () {
        return(
          <div>
              React Todo App
          </div>
        );
    }
});

module.exports = TodoApp;